{
  "version": 2,
  "buildCommand": "npm install --include=dev && npm --prefix backend install --include=dev && npm --prefix frontend install --include=dev && npm run build && npm --prefix backend run prisma:push && npm --prefix backend run seed",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/health",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
