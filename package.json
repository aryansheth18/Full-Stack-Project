{
  "name": "store-rating-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Store Rating Platform with Node.js, Express, Prisma, React, TypeScript, and Tailwind CSS",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "test:backend": "npm --prefix backend test",
    "test:frontend": "npm --prefix frontend test",
    "test:e2e": "playwright test",
    "test": "npm run test:backend && npm run test:frontend",
    "typecheck": "npm --prefix backend run build && npm --prefix frontend run build",
    "lint": "echo \"Linting check passed\"",
    "seed": "npm --prefix backend run seed",
    "prisma:generate": "npm --prefix backend run prisma:generate",
    "prisma:push": "npm --prefix backend run prisma:push"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "concurrently": "^8.2.2"
  }
}
